/* =============================================================
   BLVCK LABEL — main.js
   Vanilla JS, no framework. One file for the whole site.
   -------------------------------------------------------------
   MODULE 1 — FOUNDATION
   No interactive behaviour yet. Later modules add:
     - marquee ticker duplication (Module 2)
     - mobile nav toggle (Module 2)
     - shop filters (Module 6)
     - fake cart drawer state (Module 8)
   ============================================================= */

(function () {
  "use strict";

  // Foundation heartbeat — confirms the script is wired up with no errors.
  document.documentElement.classList.add("js");
})();
